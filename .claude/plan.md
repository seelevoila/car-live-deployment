# 竞赛提交材料更新计划

## 当前状态分析

### 已有材料（最后更新：2026-08-28）
- 01-系统设计文档.docx/pdf
- 02-汽车垂直领域RAG知识库构建方案.docx/pdf
- 03-流式TTS动态改稿实现方案.docx/pdf
- 04-语音克隆拟人化优化方案.docx/pdf
- 05-标准化功能测试报告.docx/pdf
- 10-系统设计演示.pptx
- 部署、演示、评测等markdown文档

### 系统重大更新（未反映在材料中）

#### 1. RAG语义检索升级（2026-09-08）
- **从哈希向量 → BGE语义嵌入模型**
  - BAAI/bge-small-zh-v1.5 (512维)
  - BAAI/bge-reranker-base
  - FAISS + BM25混合召回 + RRF融合
  - 父子分块策略
  - 动力范围检查与资料冲突检测
- **当前文档描述**：仍写"稳定哈希向量"和"256维"（过时）
- **影响范围**：系统设计文档、RAG方案、测试报告

#### 2. TTS引擎变化
- **官方声明**：默认使用IndexTTS-2.5
- **实际状态**：根据TTS_INDEX_TTS25_SWITCH_AUDIT.md，运行的是IndexTTS-2.0
  - 配置写2.5，实际加载2.0权重
  - 语速控制、多语言支持等2.5特性未生效
  - 验收指标不达标（19085ms vs 3000ms目标）
- **当前文档描述**：写GPT-SoVITS v2ProPlus（部分准确，但未说明实际引擎选择）
- **影响范围**：系统设计文档、TTS方案、测试报告

#### 3. 大模型接入界面（2026-09-08）
- 新增前端配置页面，无需手动编辑.env
- 支持实时保存和连接测试
- **当前文档**：未提及

## 更新策略

### 方案A：如实反映实际部署状态（推荐）
**原则**：材料描述与实际运行系统完全一致，避免评审时出现"文档说A实际是B"的情况

1. **RAG部分**：更新为BGE语义检索方案
   - 系统设计文档：更新架构图中的"哈希向量"→"BGE语义向量 + FAISS"
   - RAG方案：重写检索策略章节，增加模型选择、分块策略、重排序
   - 测试报告：保持40/40结果，说明升级后的技术栈

2. **TTS部分**：明确说明当前使用GPT-SoVITS（或如实说明IndexTTS-2.0状态）
   - 如果验收时用GPT-SoVITS：保持现有描述
   - 如果要用IndexTTS：需先解决权重版本问题，重新验收延迟指标
   - 混合方案：说明支持两种引擎切换，提交时以达标的为准

3. **大模型接入**：补充配置界面说明
   - 系统设计：增加"大模型接入层"架构说明
   - 部署文档：补充页面配置步骤

### 方案B：最小化修改
仅修正明显过时的技术描述，保持主体框架不变

## 具体实施步骤

### 第一步：确认系统当前状态
1. 运行verify.ps1，获取最新验收数据
2. 确认TTS_PROVIDER实际配置（gpt-sovits还是idextts2）
3. 确认RAG是否已使用BGE模型
4. 记录实际延迟、准确率等指标

### 第二步：更新build_materials_v2.py
修改以下函数：

#### build_system_design()
- 行297：更新检索描述，改为"BGE语义向量 + BM25混合召回 + FAISS索引"
- 行290：更新架构表，模型层增加"语义检索模型"
- 行308：更新验收指标实测值（如有变化）

#### build_rag()
- 行317-339：大幅重写
  - 增加"语义模型选择"章节
  - 更新检索策略：FAISS + BM25 + RRF + 重排序
  - 增加父子分块说明
  - 补充模型资源表（BGE embedding 512维，reranker）

#### build_tts()
- 根据实际使用情况调整：
  - 如用GPT-SoVITS：保持现状
  - 如用IndexTTS：更新描述和参数
  - 如两者都支持：说明可切换

#### build_test_report()
- 更新环境表：增加RAG模型信息
- 更新指标表：使用最新验收数据
- 补充RAG升级说明

#### build_ppt()
- 幻灯片5（RAG）：更新为BGE + FAISS
- 幻灯片6（TTS）：根据实际引擎调整
- 幻灯片10（指标）：更新实测数据

### 第三步：更新markdown文档
- 06-部署与运行说明.md：补充RAG模型安装步骤
- 07-现场演示操作手册.md：更新演示话术
- 08-音色拟人度评测表.md：保持不变或补充实际评分

### 第四步：重新生成材料
```powershell
cd submission
python build_materials_v2.py
```

### 第五步：转换PDF
使用现有的docx→pdf转换流程

### 第六步：验证一致性
- 对比文档描述与实际系统
- 在真实环境中走一遍演示流程
- 确认所有指标可复现

## 需要用户明确的问题

1. **TTS引擎选择**：提交时使用哪个引擎？
   - GPT-SoVITS（已验收通过，2323ms）
   - IndexTTS-2.5（需先安装正确权重，重新验收）
   
2. **材料更新范围**：
   - 完整更新（方案A，耗时较长但准确）
   - 关键修正（方案B，快速但可能遗漏细节）

3. **验收数据来源**：
   - 使用8月27日的旧数据（已在材料中）
   - 重新运行verify.ps1获取最新数据

## 风险提示

1. **文档与代码不一致**：如果材料更新后，现场演示时使用的是不同配置，会导致评审疑惑
2. **IndexTTS状态不明**：当前IndexTTS-2.0不达标，如果文档写2.5但实际跑2.0会暴露问题
3. **时间紧迫**：完整更新需要2-3小时，包括文档编写、PDF生成、验证

## 推荐行动

**立即执行**：
1. 运行`powershell -ExecutionPolicy Bypass -File .\verify.ps1`获取当前状态
2. 检查`backend/.env`中的`TTS_PROVIDER`值
3. 确认是否已安装BGE模型（检查models/rag目录）

**基于检查结果决定**：
- 如果RAG已升级且验收通过 → 更新RAG相关文档
- 如果TTS用GPT-SoVITS → 保持现有描述
- 如果TTS用IndexTTS → 需先修复版本问题

**最稳妥方案**：
使用当前已验收通过的配置（GPT-SoVITS + BGE RAG），更新文档以准确反映这套配置，确保现场演示与材料100%一致。
