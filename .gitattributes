* text=auto
*.sh text eol=lf
*.ps1 text eol=crlf
*.cmd text eol=crlf

# Keep the bundled RAG files as ordinary Git blobs/parts so clones do not
# depend on a repository-specific Git LFS quota.
models/rag/**/model_quantized.onnx -filter -diff -merge -text
